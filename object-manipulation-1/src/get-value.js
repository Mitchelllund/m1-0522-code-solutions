/* exported getValue */

// define a function caleld getValue with 2 parameters of object and key
// return the object at index key

function getValue(object, key) {
  return object[key];
}

// define a function caleld getValue with 2 parameters of object and key
// return the object at index key
