/* exported setValue */
// define a function called set value with 3 parameters of object, key and value
// return the object at index key set equal to value

function setValue(object, key, value) {
  var newValue = object[key] = value;
  return newValue;
}

// define a function called set value with 3 parameters of object, key and value
// create a new variable called newValue and assign it object at index ---
// --- key set equal to value
// return newValue
