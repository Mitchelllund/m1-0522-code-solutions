/* exported firstChars */
// define a function called firstChar with 2 parameters of length and string
// create a var called result and assign it the value of string.substring(0, length)
// return the var result

function firstChars(length, string) {
  return string.substring(0, length);
}

// define a function called firstChar with 2 parameters of length and string
// return substring method of the string object with 2 arguemnts 0 and length
