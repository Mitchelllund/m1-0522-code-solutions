/* exported includes */
// define a function called includes with 2 parameters of array and value
// create a for loop that loops through the array at index 0
// create a if statement that checks if array at i is strickly equal to value...
// if it does return true
// create an else statement that returns false
function includes(array, value) {
  for (var i = 0; i < array.length; i++) {
    if (array[i] === value) {
      return true;
    }
  } return false;
}

// define a function called includes with 2 parameters of array and value
// create a for loop that loops through the array at index 0
// create a if statement that checks if array at i is strickly equal to value...
// if it does return true
// return false
