/* exported isUpperCased */
// define a function called isUpperCased with a parameter of word
// create an if statement with the expression word strictly equal to ---
// toUpperCased property of the word object
// return true

function isUpperCased(word) {
  return word === word.toUpperCase();
}

// define a function called isUpperCased with a parameter of word
// return word strictly equal to toUpperCase property of word object
