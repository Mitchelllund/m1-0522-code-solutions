/* exported getLastChar */
// define a function called getLastChar with a paramter of string
// return string at index string.length - 1

function getLastChar(string) {
  var newString = string[string.length - 1];
  return newString;
}

// define a function called getLastChar with a paramter of string
// create a variable called newString with a value of string at index ---
// string.length - 1
// return newString var
